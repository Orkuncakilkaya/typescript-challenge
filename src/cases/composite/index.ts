/**
 * Create a class named Component implements CompositeContract
 */
import {ComponentConstructorParams, CompositeContract} from '../../interfaces/composite.contract';

export default function (componentInfo: ComponentConstructorParams): CompositeContract {
    throw new Error('not implemented');
}
