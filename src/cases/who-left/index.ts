/**
 * Write a function that finds compares two arrays and finds lost item
 * example: whoLeft([1,2,3], [1,3]) -> 2
 * example: whoLeft(['orkun', 'eray', 'eray', 'ahmet'], ['orkun', 'eray', 'ahmet']) -> 'eray'
 */
export default function (...args) {
}
