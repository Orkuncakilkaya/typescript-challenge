import {PersonConstructorParams, PersonContract} from '../../interfaces/person.contract';

/**
 * Create a class named Person that implements PersonContract
 */
export default function (personInfo: PersonConstructorParams): PersonContract {
    throw new Error('not implemented');
}
