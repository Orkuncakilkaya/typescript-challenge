/**
 * Given two integers as arguments, create a function that finds the largest prime within the range of the two integers.
 * example: fatPrime(2, 10) -> 7
 */
export default function (...args) {
}
